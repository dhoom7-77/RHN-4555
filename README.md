**1. الملفات الاساسيه :**

عندنا ملف `platformio.ini`. هاذا ملف خاص لمكتبت PlatformIO ماله اي علاقه ب المسابقه 

عندنا ملف `README.md` ذا الملف يشرح هيكلت البروجكت كامل

**2. مجلد خاص لبرمجه الاكواد ( برمجه الحساسات والموترات الخخ ) (`src/`):**

ذا المجلد يحتوي على الملفات الي تخص برمجه الحساسات والموترات الخخ ( C++ ) :

(بعد مانحط الملفات البرمجه بكتبها هنا)

**4. مجلد شروح القطع المستخدمه بسياره ووش مهمتها (`Explanation of Robot Assembly/`):**

ملف `Arduino Mega`: يشرح لك قطعه الاردوينو وش مهمتها في السياره

ملف `Ultrasonic`: يوضح لك وش فايده حساس المسافه وليش حطينها ووش مهمته

ملف `Main camera`: شرح للكاميرا الاساسيه ووش مهمتها وليش حطينها 

ملف `dc motor`: ذا الملف يشرح ليه حطينا الموتور 

ملف `README.md`: ملف يشرح هيكل السياره من اول اخذناها الى اخر شي سوينا فيها

ملف `Motor Driver`: يشرح ليه حطينا الموتور درايف وليش استخدمناه

ملف `Amazon 9v battery`: يشرح ليه استخدمنا بطاريه امازون 9v ووش فايدتها 

**5. مجلد صور القطع الي تم استخدامها (`Photos of the parts used/`):**

صوره `Arduino Mega.jpg`: صوره ل الاردوينو الميجا

صوره `Motor Driver.png`: صوره لقطعه موتور درايف 

صوره `Breadboard.jpg`: صوره ل البريد بورد ( لوحه التجارب )

صور `dc motor.png`: صوره dc motor ( موتور الكفر الخلفي )

صوره `Amazon 9v battery.png`: صوره لبطاريه المستخدمه في السياره

صوره `Ultrasonic.png`: صوره لحساس المسافه الي تم استخدماها في السياره

صوره `Main camera.jpg`: للكاميرا الاساسيه المستخدمه في السياره

**6. مجلد الصور ونحن نشتغل (`Pictures/`):**
